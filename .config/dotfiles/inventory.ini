# Pin localhost to the same Python that's running ansible (the uvx interpreter)
# instead of letting auto-discovery pick a version-specific mise shim like
# python3.13, which errors out when no mise version is set for it.
[local]
localhost ansible_connection=local ansible_python_interpreter={{ansible_playbook_python}}
