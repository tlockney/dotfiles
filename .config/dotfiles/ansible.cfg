[defaults]
# Use explicit localhost inventory to avoid "no inventory parsed" warnings
inventory = inventory.ini

# Opt into the new fact access pattern to avoid deprecation warnings.
# Facts must be accessed via ansible_facts.X instead of ansible_X.
# This behavior will become the default in ansible-core 2.24.
inject_facts_as_vars = false
