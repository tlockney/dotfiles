#!/usr/bin/env bash
# Wake the macOS display by asserting user activity.
# Useful for remote triggering via SSH or automation tools.
set -euo pipefail

if [[ "$(uname -s)" != "Darwin" ]]; then
    echo "Error: this script only works on macOS" >&2
    exit 1
fi

caffeinate -u -t 2
